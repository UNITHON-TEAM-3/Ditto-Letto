import Foundation
import RxSwift
import RxCocoa

class LoadingLetterVM: BaseVM {
    let disposeBag = DisposeBag()

    // MARK: - In/Out
    struct Input {
    }
    struct Output {
    }

    // MARK: - Translate
    func transform(_ input: Input) -> Output {
        let output = Output()

        return output
    }
}
